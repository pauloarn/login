import {
  StyleSheet
} from 'react-native'

export default () => StyleSheet.create({
  textInput: {
    width: '100%',
    backgroundColor: '#FAF5FF',
    borderRadius: 5,
    padding: 10,
    color: '#383E71',
    marginVertical: 10,
    borderWidth: 1
  }
})
